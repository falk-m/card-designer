{
  "scripts": {
    "start": "http-server . -p 8000",
    "build": "esbuild src/index.ts --bundle --outfile=dist/build.js",
    "dev": "esbuild src/index.ts --bundle --outfile=dist/build.js --watch"
  },
  "devDependencies": {
    "esbuild": "0.28.0",
    "http-server": "^14.1.1"
  }
}
